{
  "name": "@preparatoria/authz",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "lint": "eslint src tests",
    "typecheck": "tsc --noEmit",
    "test": "tsc --project tsconfig.build.json && node --test tests/authz.test.mjs"
  },
  "devDependencies": {
    "@preparatoria/config": "workspace:*"
  }
}
